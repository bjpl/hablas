# Environment Variables Template
# Copy this file to .env.local and fill in your actual values

# Anthropic API Configuration
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Unsplash API Configuration
NEXT_PUBLIC_UNSPLASH_ACCESS_KEY=your-unsplash-access-key-here

# Authentication Configuration
# CRITICAL SECURITY: Change these values in production!
# JWT_SECRET must be at least 32 characters long and cryptographically random
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-make-it-long-and-random-at-least-32-chars

# Admin Account Configuration
ADMIN_EMAIL=admin@hablas.co
# If ADMIN_PASSWORD is not set, a secure random password will be generated
# In production, either set this to a strong password OR let it auto-generate
# (auto-generated password will be shown once in console on first startup)
ADMIN_PASSWORD=

# CORS Configuration (Production Only)
# Set these to your actual domain(s) in production
NEXT_PUBLIC_APP_URL=https://hablas.co
ALLOWED_ORIGIN_1=https://www.hablas.co
ALLOWED_ORIGIN_2=

# Redis Configuration (Optional - for distributed rate limiting)
# If not set, in-memory rate limiting will be used
REDIS_URL=
REDIS_PASSWORD=

# PostgreSQL Database Configuration
# REQUIRED: Set one of these options for database connectivity
# Option 1: Connection String (recommended for production)
DATABASE_URL=postgresql://username:password@localhost:5432/hablas

# Option 2: Individual parameters (fallback if DATABASE_URL not set)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=hablas
DB_USER=postgres
DB_PASSWORD=your-database-password

# Database Pool Configuration
DB_POOL_MAX=20
DB_IDLE_TIMEOUT=30000
DB_CONNECT_TIMEOUT=2000
DB_SSL=false

# Node Environment
NODE_ENV=development
