# Anthropic API Configuration
# Get your API key from: https://console.anthropic.com/

# Required for AI resource generation
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional: Override default model
# AI_MODEL=claude-sonnet-4-5-20250929

# Optional: Control generation parameters
# AI_MAX_TOKENS=8000
# AI_TEMPERATURE=0.7

# Supabase Configuration (Optional - for future features)
# NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Upstash Redis Configuration (Optional for API rate limiting)
# Get your credentials from: https://console.upstash.com/
# 1. Create a new Redis database (free tier available)
# 2. Go to database details and copy the REST API credentials
# 3. Add the URL and token below
#
# Note: Rate limiting works WITHOUT Redis (in-memory fallback)
# Redis is recommended for production but not required
# UPSTASH_REDIS_REST_URL=https://your-redis-endpoint.upstash.io
# UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_rest_token_here
